# Place asl_emg_classifier.onnx here before building the app.
# Run: python scripts/train_synthetic_baseline.py  (generates models/asl_emg_classifier.onnx)
# Then: cp models/asl_emg_classifier.onnx mobile/react-native/assets/model/
