# EMG-ASL Inference Server — runtime dependencies
# Pin ranges keep installs reproducible while allowing patch-level updates.
#
# Hardware: Thalmic MYO Armband (8-ch sEMG @ 200 Hz).
# Connection via myo-python + MyoConnect (USB dongle).
# See hardware/myo_armband/README.md for setup.

# --- Core numerics & signal processing ---
numpy>=1.26,<3
scipy>=1.12

# --- Deep learning ---
torch>=2.2
torchvision
torchaudio

# --- ONNX (cross-platform model export & inference) ---
onnx>=1.15
onnxruntime>=1.17

# --- API framework ---
fastapi>=0.110
uvicorn[standard]>=0.29
websockets>=12
python-multipart>=0.0.9
pydantic>=2.6

# --- MYO Armband SDK (primary hardware interface) ---
myo-python>=0.2.1       # Python wrapper for the Myo C++ SDK (myo-python on PyPI)
cffi>=1.16              # Required by myo-python for C FFI bindings

# --- EMG / biosignal processing libraries ---
libemg>=1.0
pyemgpipeline>=1.0
neurokit2>=0.2
wfdb>=4.1               # WFDB format reader for GRABMyo dataset
mne>=1.7                # EMG epoch handling and MNE pipeline utilities

# --- ML / data science utilities ---
scikit-learn>=1.4
pandas>=2.2
optuna>=3.6
wandb>=0.17
imbalanced-learn>=0.12  # SMOTE oversampling used in loader.py balance_classes()
joblib>=1.3             # SVM model serialisation (joblib.dump / joblib.load)
tsfresh>=0.20           # Automated time-series feature extraction

# --- Visualisation ---
matplotlib>=3.8
seaborn>=0.13

# --- HTTP client (used in tests & health probes) ---
httpx>=0.27
